import XCTest
@testable import DialogCountryPicker

final class DialogCountryPickerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DialogCountryPicker().text, "Hello, World!")
    }
}
